To run this project, after you download this project, run "npm install" to install all dependencies
then, run "nom start" to start this project. if the MongoDB is not working even you already download 
the mongoDB, run "mongod --config /usr/local/etc/mongod.conf --fork"should solve the problem. 